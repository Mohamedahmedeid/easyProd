# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x, 18.x, 'latest']

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build --if-present
    - name: Set environment variables
      run: |
        echo "PORT=4040" >> .env
        echo "MONGO_URI=mongodb+srv://asedqiq8:c1mMenncxGYc81eq@cluster0.tpjatzz.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0" >> .env
        echo "JWT_SECRET=mykey" >> .env
        echo "ADMIN_USERNAME=Ahmed" >> .env
        echo "ADMIN_PASSWORD=Ahmed" >> .env
    - name: Run app.js
      run: node app.js
